# Illwill Enhance Config

The parser reads the first `jsonc` fenced block.

```jsonc
{
  // Show app header line when false.
  "disable_titlebar": false,

  // Start terminal UI in fullscreen.
  "full_screen": true
}
```
